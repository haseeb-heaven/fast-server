{
    "builds": [
        {
            "src": "server/FastServer.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "server/FastServer.py"
        }
    ]
}
